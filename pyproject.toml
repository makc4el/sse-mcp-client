[project]
name = "mcp-sse-client"
version = "0.1.0"
description = "SSE-based MCP Client for connecting to MCP servers"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.0.0",
    "mcp[cli]>=1.2.1",
    "python-dotenv>=1.0.1",
]