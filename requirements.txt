# Core MCP client dependencies
httpx==0.25.2

# LangChain integration dependencies (specific versions to avoid conflicts)
langchain==0.1.20
langchain-openai==0.1.8
langchain-core==0.1.52
langchain-community==0.0.38

# LangGraph dependencies for graph deployment
langgraph>=0.0.40

# Required for Pydantic models
pydantic>=2.0.0,<3.0.0

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Note: Python 3.8+ required for LangChain
# Avoid LangGraph dependencies by using specific LangChain versions
