# Core MCP client dependencies
httpx==0.25.2

# LangChain integration dependencies (compatible versions for LangGraph)
langchain>=0.2.0,<0.3.0
langchain-openai>=0.1.8,<0.2.0
langchain-core>=0.2.2,<0.3.0
langchain-community>=0.2.0,<0.3.0

# LangGraph dependencies for graph deployment
langgraph>=0.0.40

# Required for Pydantic models
pydantic>=2.0.0,<3.0.0

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Optional web interface dependencies
streamlit>=1.28.0  # For web chat interface

# Note: Python 3.8+ required for LangChain
# Avoid LangGraph dependencies by using specific LangChain versions
