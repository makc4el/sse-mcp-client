# Core MCP client dependencies
httpx==0.25.2

# LangChain integration dependencies
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-core>=0.1.0

# Optional: for better async support
pydantic>=2.0.0

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Note: Python 3.7+ required
# For enum support on older Python versions:
enum34; python_version < '3.4'
